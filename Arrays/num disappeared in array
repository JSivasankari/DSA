class Solution(object):
    def findDisappearedNumbers(self, nums):
        ans=[]
        seen=set(nums)
        n=len(nums)
        for i in range(1,n+1):
            if i not in seen:
                ans.append(i)
        return ans
        


        
